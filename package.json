{
  "name": "eduhub",
  "version": "1.0.0",
  "private": true,
  "description": "EduHub full-stack application",
  "scripts": {
    "backend:dev": "npm --prefix backend run dev",
    "backend:start": "npm --prefix backend start",
    "frontend:dev": "npm --prefix frontend run dev",
    "landing:dev": "npm --prefix landing-ui run dev"
  }
}
